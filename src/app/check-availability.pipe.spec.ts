import { CheckAvailabilityPipe } from './check-availability.pipe';

describe('CheckAvailabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckAvailabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
